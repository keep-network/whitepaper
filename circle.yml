general:
  artifacts:
    - "keep.pdf"

dependencies:
  pre:
    - sudo apt-get install texlive-latex-base texlive-binaries

test:
  override:
    - pdflatex -halt-on-error keep.tex
    - bibtex keep
    - pdflatex -halt-on-error keep.tex
